#### 测试项 1.2.2-T2：通用泛在处理中间件处理结果正确性测试

##### 技术指标

指标1.2（2）: 通用泛在数据一致性检测技术支持人机物融合泛在资源，且检验时间比2023年前性能最高技术（GEAS）降低 50%。

##### 测试方法

通过搭建测试环境，使用测试脚本依次启动各项测试，解析包括GPS、速度传感器等泛在资源数据，并验证中间件的处理结果正确性。

##### 测试用例

- 1.2.2-T2-1: 轻量负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载（数据规模小于5w、数据间隔大于50ms）下基于基础约束规则（单层量词结构）可以正确地处理泛在数据。轻量负载包含 49,746 条原始数据，平均数据间隔为 72.37 毫秒。基础约束规则只关心单条数据内的属性关系，使用单层量词结构。测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理结果与基准一致，验证算法的正确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_1/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_1/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_1` | 显示负载类型为"轻量负载"，数据条数为 49746，平均时间间隔为 72.37 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_1_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_1_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_1_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_1_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2-2: 中度负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载（数据规模在10w到20w之间、数据间隔在20ms到50ms之间）下基于基础约束规则（单层量词结构）可以正确地处理泛在数据。中度负载包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。基础约束规则只关心单条数据内的属性关系，使用单层量词结构。测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在面对更多数据负载时，仍能保证检测结果的准确性和完整性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_2/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_2/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_2` | 显示负载类型为"中度负载"，数据条数为 124345，平均时间间隔为 28.95 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_2_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_2_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_2_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_2_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2-3: 重度负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载（数据规模大于20w、数据间隔小于20ms）下基于基础约束规则（单层量词结构）可以正确地处理泛在数据。重度负载包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。基础约束规则只关心单条数据内的属性关系，使用单层量词结构。在大规模数据负载的压力下，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）仍让能确保数据处理的完整性和可靠性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_3/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_3/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_3` | 显示负载类型为"重度负载"，数据条数为 230628，平均时间间隔为 15.61 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_3_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_3_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_3_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_3_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2-4: 轻量负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载（数据规模小于5w、数据间隔大于50ms）下基于复杂约束规则（多层量词嵌套结构）可以正确地处理泛在数据。轻量负载包含 49,746 条原始数据，平均数据间隔为 72.37 毫秒。复杂约束规则关心多条数据间的关系，通过量词嵌套表达复杂逻辑。复杂约束规则包含更深层次的逻辑嵌套，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在约束规则复杂性增加时仍能产生正确的检测结果。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_4/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_4/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_4` | 显示负载类型为"轻量负载"，数据条数为 49746，平均时间间隔为 72.37 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_4_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_4_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_4_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_4_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2-5: 中度负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载（数据规模在10w到20w之间、数据间隔在20ms到50ms之间）下基于复杂约束规则（多层量词嵌套结构）可以正确地处理泛在数据。中度负载包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。复杂约束规则关心多条数据间的关系，通过量词嵌套表达复杂逻辑。本场景结合了复杂的规则逻辑和中度负载规模，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在综合压力下保持处理结果的准确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_5/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_5/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_5` | 显示负载类型为"中度负载"，数据条数为 124345，平均时间间隔为 28.95 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_5_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_5_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_5_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_5_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2-6: 重度负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载（数据规模大于20w、数据间隔小于20ms）下基于复杂约束规则（多层量词嵌套结构）可以正确地处理泛在数据。重度负载包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。复杂约束规则关心多条数据间的关系，通过量词嵌套表达复杂逻辑。这是正确性测试中最严苛的场景，同时面对最复杂的规则和最大的负载。测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在极限条件下仍能保证结果的正确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/T2_6/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | 张灵毓 |
| **设计日期** | 2026.01.27 |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备： `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录： `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：在 `src/test/resources/T2_6/` 目录下确保以下文件存在：- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查： `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 运行负载分析： `./run_tests.sh stats T2_6` | 显示负载类型为"重度负载"，数据条数为 230628，平均时间间隔为 15.61 ms | 与预期结果一致 |
| 6 | 编译项目代码： `./run_tests.sh compile` | 输出编译流程信息，显示"编译完成"提示 | 编译成功，无错误 |
| 7 | 运行基准泛在数据处理方法正确性测试： `./run_tests.sh test T2_6_Baseline` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_6_Baseline 测试通过"提示 | 与预期结果一致 |
| 8 | 运行融合式泛在数据处理方法正确性测试： `./run_tests.sh test T2_6_Fusion` | 显示成功解析的泛在资源数据列表，显示"处理结果验证成功"以及"T2_6_Fusion 测试通过"提示 | 与预期结果一致 |