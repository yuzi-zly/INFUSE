#### 测试项 1.2.2：通用泛在处理中间件

##### 技术指标

通用泛在数据处理效率提升 50%。

##### 测试方法

通过搭建测试环境，使用测试脚本依次启动各项测试，验证中间件的功能正确性和效率提升情况。

##### 测试用例

- 1.2.2-T1: 轻量负载下基于基础约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于基础约束规则，能够智能地将泛在环境中的数据变化构建为高效的处理任务批次。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在轻量负载下能针对基础约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase1/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase1/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`（当前最先进的静态任务构建方法基准）<br>- `oracle_taskout_Fusion.txt`（本项目的融合式任务构建方法基准文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T1_Baseline` | 显示"验证通过"和"T1_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T1_Fusion` | 显示"验证通过"和"T1_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2: 中度负载下基于基础约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于基础约束规则的任务构建能力。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。随着负载增加，测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在中度负载下能针对基础约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase2/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase2/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`<br>- `oracle_taskout_Fusion.txt` | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T2_Baseline` | 显示"验证通过"和"T2_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T2_Fusion` | 显示"验证通过"和"T2_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T3: 重度负载下基于基础约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于基础约束规则的任务构建能力。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在重度负载下能针对基础约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase3/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase3/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`<br>- `oracle_taskout_Fusion.txt` | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T3_Baseline` | 显示"验证通过"和"T3_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T3_Fusion` | 显示"验证通过"和"T3_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T4: 轻量负载下基于复杂约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于复杂约束规则的任务构建能力。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。复杂约束规则具有更深的逻辑嵌套和更复杂的依赖关系，测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在轻量负载下能针对复杂约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase4/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase4/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`<br>- `oracle_taskout_Fusion.txt` | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T4_Baseline` | 显示"验证通过"和"T4_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T4_Fusion` | 显示"验证通过"和"T4_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T5: 中度负载下基于复杂约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于复杂约束规则的任务构建能力。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。本场景综合考验系统对复杂约束规则和中度负载的处理能力，测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在中度负载下能针对复杂约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase5/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase5/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`<br>- `oracle_taskout_Fusion.txt` | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T5_Baseline` | 显示"验证通过"和"T5_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T5_Fusion` | 显示"验证通过"和"T5_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T6: 重度负载下基于复杂约束规则的任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于复杂约束规则的任务构建能力。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。这是最具挑战性的压力测试场景，结合了最复杂的规则逻辑和最大的数据负载。测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）在重度负载下能针对复杂约束规则合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase6/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase6/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`<br>- `oracle_taskout_Fusion.txt` | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T6_Baseline` | 显示"验证通过"和"T6_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T6_Fusion` | 显示"验证通过"和"T6_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T7: 轻量负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于基础约束规则可以正确地处理泛在数据。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理结果与基准一致，验证算法的正确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase7/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase7/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T7_Baseline` | 显示"验证通过"和"T7_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T7_Fusion` | 显示"验证通过"和"T7_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T8: 中度负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于基础约束规则可以正确地处理泛在数据。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。随着负载的增长，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在面对更多数据负载时，仍能保证检测结果的准确性和完整性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase8/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase8/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T8_Baseline` | 显示"验证通过"和"T8_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T8_Fusion` | 显示"验证通过"和"T8_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T9: 重度负载下基于基础约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于基础约束规则可以正确地处理泛在数据。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。在大规模数据负载的压力下，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）仍让能确保数据处理的完整性和可靠性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase9/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase9/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T9_Baseline` | 显示"验证通过"和"T9_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T9_Fusion` | 显示"验证通过"和"T9_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T10: 轻量负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于复杂约束规则可以正确地处理泛在数据。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。复杂约束规则包含更深层次的逻辑嵌套，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在约束规则复杂性增加时仍能产生正确的检测结果。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase10/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase10/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T10_Baseline` | 显示"验证通过"和"T10_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T10_Fusion` | 显示"验证通过"和"T10_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T11: 中度负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于复杂约束规则可以正确地处理泛在数据。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。本场景结合了复杂的规则逻辑和中度负载规模，测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在综合压力下保持处理结果的准确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase11/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase11/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T11_Baseline` | 显示"验证通过"和"T11_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T11_Fusion` | 显示"验证通过"和"T11_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T12: 重度负载下基于复杂约束规则的处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于复杂约束规则可以正确地处理泛在数据。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。这是正确性测试中最严苛的场景，同时面对最复杂的规则和最大的负载。测试项目要求基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）在极限条件下仍能保证结果的正确性。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase12/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase12/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T12_Baseline` | 显示"验证通过"和"T12_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T12_Fusion` | 显示"验证通过"和"T12_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T13: 轻量负载下基于基础约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于基础约束规则的性能表现。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。本场景作为效率测试的基准场景，测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）相比基准泛在数据处理方法（自适应任务构建方法GEAS，成果发表于TSE'21）的处理效率提升 50% 以上，验证新方法的优化效果。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase13/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase13/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T13_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T13_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |

- 1.2.2-T14: 中度负载下基于基础约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于基础约束规则的性能表现。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。随着负载增加到中度，测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）在处理更多数据变化时，仍能保持显著的效率优势，确保在实际应用场景中能够带来性能提升，相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）效率提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase14/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase14/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T14_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T14_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |

- 1.2.2-T15: 重度负载下基于基础约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于基础约束规则的性能表现。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**基础约束规则**只关心单条数据内的属性关系，使用单层量词结构。在重度数据处理压力下，测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）展现出更大的性能优势，证明融合式方法在高负载场景下的可扩展性和高效性，相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）效率提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase15/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase15/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T15_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T15_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |

- 1.2.2-T16: 轻量负载下基于复杂约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在轻量负载下基于复杂约束规则的性能表现。**轻量负载**包含 111,600 条原始数据，平均数据间隔为 32.26 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。复杂约束规则的深层逻辑嵌套为处理带来挑战，测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）通过智能任务构建和高效并发处理，在规则复杂度增加时仍能实现显著的效率提升，相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）效率提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase16/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase16/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T16_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T16_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |

- 1.2.2-T17: 中度负载下基于复杂约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在中度负载下基于复杂约束规则的性能表现。**中度负载**包含 124,345 条原始数据，平均数据间隔为 28.95 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。本场景综合了复杂约束规则和中度负载的双重挑战，测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）在实际应用最常见的场景下展现出稳定的性能优势，证明方法的实用价值，相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）效率提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase17/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase17/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T17_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T17_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |

- 1.2.2-T18: 重度负载下基于复杂约束规则的处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在重度负载下基于复杂约束规则的性能表现，这是最具挑战性的压力测试场景。**重度负载**包含 230,628 条原始数据，平均数据间隔为 15.61 毫秒。**复杂约束规则**关心多条数据间的关系，通过量词嵌套表达复杂逻辑，所使用的约束规则至多两层嵌套。测试项目要求本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）在面对最复杂的规则逻辑和最大的负载时，仍能通过智能批处理、虚拟真值传播和高度并发处理实现显著的效率提升，充分展现融合式方法在极限场景下的性能优势，相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）效率提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase18/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase18/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T18_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T18_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |
