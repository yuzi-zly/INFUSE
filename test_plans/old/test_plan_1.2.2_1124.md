#### 测试项 1.2.2：通用泛在处理中间件

##### 技术指标

通用泛在数据处理效率提升 50%。

##### 测试方法

通过搭建测试环境，使用测试脚本依次启动各项测试，验证中间件的功能正确性和效率提升情况。

##### 测试用例

- 1.2.2-T1: 泛在数据处理任务构建正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件能够智能地将泛在环境中的海量数据变化构建为高效的处理任务批次，测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新任务构建方法（融合式任务构建）是否能合理组织处理任务，确保任务构建结果符合预期。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase1/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase1/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `oracle_taskout_Baseline.txt`（当前最先进的静态任务构建方法基准）<br>- `oracle_taskout_Fusion.txt`（本项目的融合式任务构建方法基准文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准任务构建方法正确性测试：<br> `./run_tests.sh test T1_Baseline` | 显示"验证通过"和"T1_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式任务构建方法正确性测试：<br> `./run_tests.sh test T1_Fusion` | 显示"验证通过"和"T1_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T2: 泛在数据处理结果正确性测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件可以正确地处理泛在数据。测试项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）和本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理结果是否正确（与基准结果一致）。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase2/` 目录；oracle 基准文件已准备。|
| **测试过程终止条件** | 各步骤测试结果和期望结果都一样，则正常终止。若不一致则记录问题，异常终止。 |
| **用例结果评估标准** | 能够成功执行用例且与预期结果一致为"通过"，否则为"不通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase2/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件）<br>- `results_oracle.txt`（基准处理结果文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行基准泛在数据处理方法正确性测试：<br> `./run_tests.sh test T2_Baseline` | 显示"验证通过"和"T2_Baseline 测试通过"提示 | 与预期结果一致 |
| 7 | 运行融合式泛在数据处理方法正确性测试：<br> `./run_tests.sh test T2_Fusion` | 显示"验证通过"和"T2_Fusion 测试通过"提示 | 与预期结果一致 |

- 1.2.2-T3: 泛在数据处理效率测试

| 项目 | 内容 |
|------|------|
| **测试目的** | 验证中间件在处理大规模泛在环境数据时的性能表现，确保本项目的最新融合式泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上。 |
| **用例初始化** | 安装JDK 17并设置脚本执行权限 |
| **前提和约束** | JDK 17已安装，已设置脚本执行权限；测试数据和配置文件已位于 `src/test/resources/testcase3/` 目录；项目代码已编译。 |
| **测试过程终止条件** | 完成所有性能测试场景，收集性能数据。 |
| **用例结果评估标准** | 本项目的最新泛在数据处理方法（融合式任务构建+融合式任务处理）的处理效率相比项目要求基准任务构建方法（自适应任务构建方法GEAS，成果发表于TSE'21）提升 50% 以上为"通过" |
| **设计人员** | TODO |
| **设计日期** | TODO |

**操作过程：**

| 步骤 | 操作 | 预期结果 | 评估准则 |
|------|------|----------|----------|
| 1 | 环境准备：<br> `java -version` 检查Java版本 | 显示JDK 17版本信息 | 与预期结果一致 |
| 2 | 进入项目目录：<br> `cd UDPE` | 成功进入项目目录 | 与预期结果一致 |
| 3 | 准备测试数据：<br>在 `src/test/resources/testcase3/` 目录下确保以下文件存在：<br>- `Bfunction.java`，`Mfunction.java`，`rules.xml`，`patterns.xml`，`data.txt`（资源文件） | 所需文件均存在 | 与预期结果一致 |
| 4 | 脚本权限检查：<br> `chmod +x run_tests.sh` | 脚本具有执行权限 | 与预期结果一致 |
| 5 | 编译项目代码：<br> `./run_tests.sh compile` | 输出编译信息，显示"编译完成"提示 | 编译成功，无错误 |
| 6 | 运行性能测试：<br> `./run_tests.sh test T3_Baseline` | 输出Baseline方法执行时间`time_Baseline` | 与预期结果一致 |
| 7 | 运行性能测试：<br> `./run_tests.sh test T3_Fusion` | 输出本项目最新泛在数据处理方法执行时间`time_Fusion` | 与预期结果一致 |
| 8 | 计算最新泛在数据处理方法相对于Baseline方法的效率提升`(time_Baseline - time_Fusion)/time_Fusion` | 以百分数的形式输出效率提升，结果显示效率提升大于50% | 与预期结果一致 |
